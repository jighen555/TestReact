import React from 'react'
import "./Button.css"

const s = "Codsdsa"

const Button = () => {
    return (
        <div class="button-custom">
            <button>{s}</button>
        </div>
    )
}

export default Button
